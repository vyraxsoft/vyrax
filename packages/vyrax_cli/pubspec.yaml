name: vyrax_cli
description: Flutter architecture and performance analysis CLI with smart init and project diagnostics.
version: 0.1.4
repository: https://github.com/vyraxsoft/vyrax

environment:
  sdk: ^3.9.0

dependencies:
  vyrax_core: ^0.1.4
  vyrax_engine: ^0.1.4
  vyrax_rules: ^0.1.4
  yaml: ^3.1.3

dev_dependencies:
  test: ^1.26.3

executables:
  vyrax: vyrax_cli
